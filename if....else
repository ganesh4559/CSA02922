#include <stdio.h>

int main() 
{ int a,b;
printf("enter the values of a and b is=\n");
scanf("%d%d",&a,&b);
if(a<=b)
{
    if(a==b)
    {
     printf("result :%d=%d",a,b);
     
    }
    else 
    {
        printf("result :%d<%d",a,b);
    }
}
else
{
    printf("result:%d>%d" ,a,b);
}
return 0;    
}
