#include <stdio.h>
int  swap( int x,int y)
{
int temp = x;
x=y;
y=temp;
}
int main ()
{
    int a=12,b=34;
    printf("before swaping a=%d and b=%d\n",a,b);
    swap(a,b);
    printf("after swaping a=%d and b=%d \n",a,b);
    return 0;
    
}
