#include <stdio.h>
#include <string.h>
int main()
{
    char str1[100]="mahesh";
    char str2[100]="ramesh ";
    char ch,temp;
    int i, j,len1,len2;
    len1=strlen(str1);
    len2=strlen(str2);
    if(len1!=len2)
    {
        printf("given strinh ard not isograms");
    }
    else
    {
        printf("given string are isograms");
    } return 0;
}
